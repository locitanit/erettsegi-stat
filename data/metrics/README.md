# metrics/

Ide kerülnek az előre aggregált, a frontend által közvetlenül kirajzolt JSON-ok
(`excel_functions.json`, `sql_keywords.json`, `points_by_topic.json`, ...).
Az 1. fázistól kezdve generálja a `build_metrics.py`.
